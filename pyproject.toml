[project]
name = "patch-code-agent"
version = "0.1.0"
description = "A test-driven coding agent harness built with LangGraph"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "langchain>=1.0",
    "langgraph>=1.0",
    "langgraph-checkpoint-sqlite>=3.1,<4",
    "pydantic>=2.10",
    "python-dotenv>=1.0",
    "rich>=13.9",
    "typer>=0.15",
]

[project.scripts]
patch-code-agent = "patch_code_agent.cli:app"

[dependency-groups]
dev = [
    "pytest>=8.3",
    "ruff>=0.9",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel.force-include]
"examples/tiny_repo" = "patch_code_agent/fixtures/cart_discount"

[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.ruff]
line-length = 100
target-version = "py312"
